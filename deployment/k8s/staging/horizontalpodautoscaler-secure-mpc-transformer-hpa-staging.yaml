apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
    name: "secure-mpc-transformer-hpa-staging"
    namespace: "secure-mpc-transformer-staging"
spec:
    scaleTargetRef:
      apiVersion: "apps/v1"
      kind: "Deployment"
      name: "secure-mpc-transformer-staging"
    minReplicas: 3
    maxReplicas: 9
    metrics:
      - 
        type: "Resource"
        resource:
          name: "cpu"
          target:
            type: "Utilization"
            averageUtilization: 70
      - 
        type: "Resource"
        resource:
          name: "memory"
          target:
            type: "Utilization"
            averageUtilization: 80
